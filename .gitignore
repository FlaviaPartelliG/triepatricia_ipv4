# build artifacts
bin/
*.o
*.exe
*.asan
*.tsan

# benchmark outputs (regenerated by `make bench`/`make graphs`)
bench/*.csv
bench/*.png

# keep the generated table out of noise if regenerated; comment out to commit it
# data/prefixes.txt

# editor / OS
*.swp
.DS_Store
